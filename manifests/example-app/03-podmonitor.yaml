apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  labels:
    app: example-app
  name: example-app
spec:
  selector:
    matchLabels:
      app: example-app
  podMetricsEndpoints:
  - port: web